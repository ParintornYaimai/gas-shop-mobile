import 'package:flutter/material.dart';

class NotificationNewsView extends StatelessWidget {
  const NotificationNewsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
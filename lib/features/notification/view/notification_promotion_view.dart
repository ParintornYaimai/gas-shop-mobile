import 'package:flutter/material.dart';

class NotificationPromotionView extends StatelessWidget {
  const NotificationPromotionView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}